# Redefine properties
# required metadata
sonar.projectKey=com.sernet:verinice.gs.service
sonar.projectName=verinice Service
sonar.projectVersion=1.11

# path to source directories (required)
sonar.sources=sernet.gs.service/src
 
# path to test source directories (optional)
#sonar.tests=testDir1,testDir2
 
# path to project binaries (optional), for example directory of Java bytecode
sonar.binaries=sernet.gs.service/bin
 
# optional comma-separated list of paths to libraries. Only path to JAR file and path to directory of classes are supported.
# sonar.libraries=
 
# The value of the property must be the key of the language.
sonar.language=java
 
# Additional parameters

# GC overhead Limit exceeded while running sonar runner
# http://stackoverflow.com/questions/24362982/gc-overhead-limit-exceeded-while-running-sonar-runner
# DM, 2015-09-10
sonar.dynamicAnalysis=false
sonar.skipDesign=true
sonar.skipPackageDesign=true